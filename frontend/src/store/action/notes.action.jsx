import axios from "axios";
import { saveNote, getNote, isMember } from "../reducer/notes.slice";

export const saveNoteasync = () => async () => {
  try {
    //http://localhost:3000/api/notes/groups/${groupId}
  } catch (error) {}
};
export const getNoteasync = () => async () => {
  try {
  } catch (error) {}
};
